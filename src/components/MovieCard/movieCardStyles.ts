import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  cardFocused: {
    borderWidth: 3,
    borderColor: '#b0e0e6',
  },
  cardDefault: {
    borderWidth: 0,
  },
});

export default styles;
